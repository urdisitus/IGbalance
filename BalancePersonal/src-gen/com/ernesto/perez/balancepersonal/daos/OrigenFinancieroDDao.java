package com.ernesto.perez.balancepersonal.daos;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import com.ernesto.perez.balancepersonal.datos.entidades.OrigenFinancieroD;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table ORIGEN_FINANCIERO_D.
*/
public class OrigenFinancieroDDao extends AbstractDao<OrigenFinancieroD, Long> {

    public static final String TABLENAME = "ORIGEN_FINANCIERO_D";

    /**
     * Properties of entity OrigenFinancieroD.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Nombre = new Property(1, String.class, "nombre", false, "NOMBRE");
        public final static Property Saldo = new Property(2, double.class, "saldo", false, "SALDO");
        public final static Property ResImagen = new Property(3, long.class, "resImagen", false, "RES_IMAGEN");
    };

    private DaoSession daoSession;


    public OrigenFinancieroDDao(DaoConfig config) {
        super(config);
    }
    
    public OrigenFinancieroDDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'ORIGEN_FINANCIERO_D' (" + //
                "'_id' INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "'NOMBRE' TEXT NOT NULL ," + // 1: nombre
                "'SALDO' REAL NOT NULL ," + // 2: saldo
                "'RES_IMAGEN' INTEGER NOT NULL );"); // 3: resImagen
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'ORIGEN_FINANCIERO_D'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, OrigenFinancieroD entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getNombre());
        stmt.bindDouble(3, entity.getSaldo());
        stmt.bindLong(4, entity.getResImagen());
    }

    @Override
    protected void attachEntity(OrigenFinancieroD entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public OrigenFinancieroD readEntity(Cursor cursor, int offset) {
        OrigenFinancieroD entity = new OrigenFinancieroD( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getString(offset + 1), // nombre
            cursor.getDouble(offset + 2), // saldo
            cursor.getLong(offset + 3) // resImagen
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, OrigenFinancieroD entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setNombre(cursor.getString(offset + 1));
        entity.setSaldo(cursor.getDouble(offset + 2));
        entity.setResImagen(cursor.getLong(offset + 3));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(OrigenFinancieroD entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(OrigenFinancieroD entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
